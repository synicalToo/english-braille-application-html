export const startGame = (canvasRef, setImageSrc) => {
  console.log("Game has started!");
};

export const startFreeTyping = (canvasRef, setImageSrc) => {
  console.log("Free Typing has started!");
};
